HTML - CONTENT
CSS - APPEARANCE/ STYLING
JS - INTERACTIVE/ LOGIC

What is JS?

- Giving instructions to the computer
- Making things interactive
- The computer follows our instructions

To get popup on screen - alert('hello');

// We used JS to modify the webpage (Basically the entire body in this case)
document.body.innerHTML = 'Hello';

// Numbers and Math
Operator Precedence and Associativity [ Use brackets to control the order of calculation ]

(...)

- /

* -

- / Same (L-R)

* - Same (L-R)

Comparision Operators

JS Behavior for Float
Computer can't store all the floats || Calculation with floats is sometimes inaccurate
0.1+0.2 = 0.30000....0004

--> Math.round(2.2) == 2
--> Math.round(2.5) == 3

To round 2.2222 to 2 decimal places
--> Math.round(2.2222 \* 100) / 100
or --> 2.2222.toFixed(2)

--> Math.floor(2.22) // 2
--> Math.ceil(2.22) // 3

--> typeof() // tells the type of a variable/ expression

--> 'hello'+3 // converted to string 'hello3' (Type coercion)

In JS, 3 ways to create a string
'Hello'
"Hello"
`Hello` String Template Literals
String Template Literals helps us with Interpolation
`Items (${1+1}): $${(2095+799)/100}`
In interpolation an expression can be embedded in a placeholder {}
`some
text` --> some\ntext

--> 'I\'am doing an assignment'
--> alert('some\ntext');

Comment in JS --> // This is a comment
Multi Line Comment -->  
/_
Hi
_/

Comment in HTML --> <!---->

3 ways to create variable in JS
let x = 10
const x = 10 // We can't change its value later after assignment
var x = 10

--> Booleans in JS
true | false

=== Equals || == Equals (try to convert both expressions to same type)
!== not equal to

and operator &&
or operator ||
not operator !

--> var doesn't follow the scoping roles

'text'/5 = NaN
--> NaN (Not a Number)

let variable1;
console.log(variable1) // Undefined

--> ternary opeator 5>3?okay:no;
Short circuit of && Operator


Objects: Groups multiple elements together

// We use objects to make our code organized
// Objects allow us to use multiple values together

//  null means we intentionally want smthg to be empty

Document Object Model --> DOM
// HTML elements are js objects